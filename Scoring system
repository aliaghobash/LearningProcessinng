//Make a scoring system using loops and arrays

float x = width/6;
float y = height/6;

void setup() {
  size(600, 600);
  background(0);
  //rectMode(CENTER);
};
void draw() {
  for (int i=0; i<array.length; i+=105) {
      rect(x+i, y, 100, 100);
  };

};
